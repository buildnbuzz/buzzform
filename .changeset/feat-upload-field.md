---
"@buildnbuzz/form-core": patch
---

Introduce core support for the upload field type.

- Add upload field type definition and core validation rules (maxSize, minSelected, maxSelected) in `@buildnbuzz/form-core`.
